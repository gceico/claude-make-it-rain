---
// Personal reflection panel. Empty and hidden by default; the client fills and
// reveals it only when the page is opened with ?tag=<yourTag> (the app's "View
// leaderboard…" menu item does this). It shows your spend, what it bought in
// everyday terms, your share of the collective total, and the question the whole
// app is built around: was it worth building it? See lib/leaderboard.ts.
---

<section class="reflect" id="reflect" aria-label="Your spend today" hidden></section>
